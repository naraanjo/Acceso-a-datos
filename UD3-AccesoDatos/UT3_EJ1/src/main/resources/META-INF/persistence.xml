<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence https://jakarta.ee/xml/ns/persistence/persistence_3_2.xsd"
             version="3.1">

    <!-- Nombre del PU (ajústalo a tu proyecto si quieres) -->
    <persistence-unit name="shop-ec" transaction-type="RESOURCE_LOCAL">

        
      	<class>model.Cliente</class>
	    <class>model.InformacionFiscal</class>
	    <class>model.Compra</class>
	    <class>model.Articulo</class>
	    <class>model.ArticuloCompra</class>
	       
        

        <properties>

			<!--  Variables filtradas desde Maven (profiles) -->
            <property name="jakarta.persistence.jdbc.driver" value="org.mariadb.jdbc.Driver"/>
            <property name="jakarta.persistence.jdbc.url" value="${db.url}"/>
            <property name="jakarta.persistence.jdbc.user" value="${db.user}"/>
            <property name="jakarta.persistence.jdbc.password" value="${db.pass}"/>
            <property name="hibernate.dialect" value="${db.dialect}"/>

			<!--  Opciones de Hibernate -->
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>
            <property name="hibernate.hbm2ddl.auto" value="validate"/>
			<!-- Opciones posibles: validate | update | create | create-drop -->
        </properties>

    </persistence-unit>

</persistence>
